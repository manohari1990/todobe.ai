version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 24
    commands:
      - cd todobe.ai
      - npm ci

  build:
    commands:
      - echo "Node.js application build completed successfully!"

artifacts:
  base-directory: todobe.ai
  files:
    - '**/*'
  exclude-paths:
    - '.env'